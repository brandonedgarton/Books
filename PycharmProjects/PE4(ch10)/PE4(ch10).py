class Employee:
    def __init__(self, name, id_number, department, job_title):
        self.set_name(name)
        self.set_id_number(id_number)
        self.set_name(name)
        self.set_name(name)